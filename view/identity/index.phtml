<?php
$paginator = $this->paginator;
$columns = array(
    'id' => 'Id',
    'ownerId' => 'Owner',
    'created' => 'Created',
);
?>
<table>
    <thead>
        <tr>
        <?php foreach($columns as $columnId => $columnLabel):?>
            <td><?=$columnLabel?></td>
        <?php endforeach;?>
        </tr>
    </thead>
    <tbody>
        <?php foreach($paginator as $item):?>
            <tr>
                <td><a href="<?=$this->url('KapitchiIdentity/identity/update', array('id' => $item['id']))?>">Edit</a></td>
                <?php foreach($columns as $columnId => $columnLabel):?>
                <td><?=$item[$columnId]?></td>
                <?php endforeach?>
            </tr>
        <?php endforeach;?>
    </tbody>
</table>
<?=$this->paginationControl($paginator,
                            'Sliding',
                            'paginator'); ?>
